<h1>Create a new review</h1>

<div class="form-group">
  <%= simple_form_for([@restaurant, @review]) do |f| %>
    <%= f.input :content, class: "form-control" %>
    <%= f.input :rating, collection: Review::RAING %>
    <%= f.submit %>
  <% end %>
</div>
